Front end of SQLITE database:

	-Tokenizer
	-Parser
	-Code generator

Back end of the SQLITE database:

	-virtual machine (takes bytecode generated by front-end and acts as a big switch statement on the type of bytecode instruction)
	-B-tree (retrieve a page from disk or save it back by issuing commands to pager)
	-pager (keeps a cache of recently-accessed pages in memory and reads or writes pages of data)
	-os interface 
